{
  "framework": null,
  "installCommand": "npm install",
  "buildCommand": "node -e \"var c=require('child_process');c.execFileSync('npm',['install'],{cwd:'frontend',stdio:'inherit'});c.execFileSync('npm',['run','build'],{cwd:'frontend',stdio:'inherit'})\"",
  "outputDirectory": "dist",
  "functions": {
    "api/index.ts": {
      "memory": 1024,
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
